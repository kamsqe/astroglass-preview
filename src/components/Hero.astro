---
/**
 * Hero Wrapper Component
 * Dynamically switches between different hero versions.
 */
import HeroLiquid from './hero/HeroLiquid.astro';
import HeroGlass from './hero/HeroGlass.astro';
import HeroNeo from './hero/HeroNeo.astro';
import HeroLuxury from './hero/HeroLuxury.astro';
import HeroMinimal from './hero/HeroMinimal.astro';

export interface Props {
  version?: 1 | 2 | 3 | 4 | 5;
}

const { version = 1 } = Astro.props;
---

{version === 1 ? <HeroLiquid /> : 
 version === 2 ? <HeroGlass /> : 
 version === 3 ? <HeroNeo /> : 
 version === 4 ? <HeroLuxury /> : 
 <HeroMinimal />}
